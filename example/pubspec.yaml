name: dio_network_toolkit_example
description: Example app for dio_network_toolkit

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  dio_network_toolkit:
    path: ../
